#ifndef __wifi_h__
#define __wifi_h__

/**
 * @brief connect to wifi
 * 
 * @param[in] ssid the ssid of the wifi
 * 
 * @param[in] pass the password of the wifi
*/
void wifiConnect(const char *ssid, const char *pass);

#endif